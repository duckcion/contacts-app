{
  "name": "contacts-book-app",
  "version": "1.0.0",
  "description": "A minimal contacts-book app with CRUD operations",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --coverage.provider=istanbul",
    "db:seed": "node src/seed.js",
    "build": "echo 'No build step required for Node.js backend'"
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4",
    "vitest": "^1.3.1"
  }
}
